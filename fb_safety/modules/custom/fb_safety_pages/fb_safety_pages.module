<?php
/**
 * @file
 * Code for the FB Safety pages feature.
 */

include_once 'fb_safety_pages.features.inc';

/**
 * Implements hook_ctools_plugin_directory().
 */
function fb_safety_pages_ctools_plugin_directory($module, $plugin) {
  if ($module == 'ctools') {
    return 'plugins/' . $plugin;
  }
}
